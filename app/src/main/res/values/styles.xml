<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="myTransparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/heychat_transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <!--<item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>-->
        <item name="android:windowContentTransitions">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--点击二维码放大的界面样式-->
    <style name="Transparent" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="dialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <!--去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--这个是定义contentoverlay的背景的-->
        <item name="android:windowContentOverlay">@null</item>
        <!--dialog之外的背景true是暗 false是亮-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--给人程序启动慢的感觉，界面会一次性刷出来，刷新同步。否则会启动的同时加载一些东西-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/AnimBottom150ms</item>
    </style>

    <style name="AnimBottom150ms">
        <item name="android:windowEnterAnimation">@anim/bottom_in_150_overshoot</item>
        <item name="android:windowExitAnimation">@anim/bottom_out_150</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <!--指示器的颜色-->
        <item name="tabIndicatorColor">?attr/colorAccent</item>
        <!--指示器（下面划线）的高度，不要就设为0-->
        <item name="tabIndicatorHeight">2dp</item>
        <!--每项左边的间距-->
        <item name="tabPaddingStart">0dp</item>
        <!--每项右边的间距-->
        <item name="tabPaddingEnd">0dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <!--标题颜色，使用下面的自定义-->
        <item name="tabSelectedTextColor">?android:textColorPrimary</item>
    </style>
    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <!--如果要用图标就要把下面改成false-->
        <item name="textAllCaps">false</item>
    </style>


</resources>
